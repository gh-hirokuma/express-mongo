<!DOCTYPE html>
<html lang="en">
  <%- include('../_partial/head') %>

  <body>
    <header class="w-full bg-gray-200 p-2 px-4 text-black flex justify-between items-center">
      <h1 class="text-xl font-extrabold tracking-widest ml-4 font-mono text-blue-700">go2dive</h1>
      <a href="/signin" class="uppercase p-2 mr-4"><form action="/signout" method="post">
        <input
          type="submit"
          value="Log Out"
        />
      </form>
    </a>
    </header>

    <section class="relative w-full h-screen">
        <div class="absolute w-full h-full bg-gray-700 opacity-50"></div>
          <div class="absolute bg-transparent w-full h-full flex flex-col justify-center">
          <div class="container flex px-4 py-3 justify-around absolute top-0">
            <button class="mr-4 bg-gray-300 hover:bg-gray-100 opacity-75 text-blue-600 uppercase text-xl font-mono py-1 px-1 rounded"><a href="/users/profile">My Profile</a></button>
            <button class="mr-4 bg-gray-300 hover:bg-gray-100 opacity-75 text-blue-600 uppercase text-xl font-mono py-1 px-1 rounded"><a href="/users/<%= user._id %>/logs">My Divelog</a></button>
            <button class="mr-4 bg-gray-300 hover:bg-gray-100 opacity-75 text-blue-600 uppercase text-xl font-mono py-1 px-1 rounded"><a href="/spots">Find Spots</a></button>
            <button class="bg-gray-300 hover:bg-gray-100 opacity-75 text-blue-600 uppercase text-xl font-mono py-1 px-1 rounded"><a href="/users">Find Buddies</a></button>
          </div>
          <% if(messages.success){ %>
            <div class="alert alert-success text-white text-5xl font-mono align-center role="alert"><%=messages.success %></div>
            <% } %>

            <div class="flex flex-col justify-center text-center">
            <small class="text-white uppercase text-xl font-mono tracking-widest">Log Your Dive, Find Dive Spots.</small><br/>
            <small class="text-white uppercase text-lg font-mono tracking-widest">Be Connected With Divers Around the World</small>
            <h1 class="text-white text-5xl font-mono mb-12 bold">Dive as You Live!</h1>
            </div>
        </div>
        <img
          class="object-cover w-full h-full"
          src="https://images.unsplash.com/photo-1569933132718-d29088ecb9fe?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=2689&q=80"
          alt="turtle"
        />
    </section>

    
  <body>
    <%- include('../_partial/header') %>
    <div>
      <p class="text-size:x-large"><%= result.username %></p>
      <p><%= result.certificate %></p>
      <p>Country: <%= result.country %></p>
      <a href="/users/<%= result._id %>/logs"
        ><%= result.username %>'s Dive Log</a
      >
    </div>

    <div>
      <a href="/users/<%= result._id %>/edit">Edit Profile</a>
      <form action="/users/<%= result._id %>?_method=DELETE" method="POST">
        <input type="submit" value="Leave go2dive" />
      </form>
    </div>

    <form class="flex flex-col" action="/signout" method="post">
      <input
        class="text-blue-800 p-4 py-2 margin-2 padding-2"
        type="submit"
        value="Sign Out"
      />
    </form>
    <%- include('../_partial/footer') %>
  </body>
</html>
